# Rust
/target
**/target

# (Cargo.lock được commit — đây là app/bin, không phải library)

# SQLite databases
*.db
*.db-*
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite-*

# App data dir (local dev)
/data

# Environment files (never commit secrets/config)
.env
.env.*
!.env.example

# Editor/OS cruft
.DS_Store
*.swp
.idea/
.vscode/
*.orig